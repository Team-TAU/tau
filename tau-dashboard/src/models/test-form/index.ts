import channelUpdate from './channel-update.form';
import { FormEleDef } from './interfaces';

const forms: { [key: string]: FormEleDef[] } = {
  'channel-update': channelUpdate,
};

export default forms;
